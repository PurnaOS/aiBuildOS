---
type: TestCase
id: TC-0119
title: "Supervision level sets the mapped option, per session and on change"
state: draft
owner: srini
provenance: agent
created: 2026-08-21
generated: { by: "claude-code", at: 2026-08-21T00:00:00Z }
kind: automated
binding: "apps/desktop/src/main/sessions.test.ts"
tags: [supervision, sessions, unit]
links:
  verifies: [RQ-0050#AC-1, RQ-0050#AC-2, ST-0067]
---

# TC-0119 — Supervision level sets the mapped option, per session and on change

Verifies [RQ-0050](../requirements/rq-0050.md) AC-1 and AC-2 at the registry seam, against the
real stub subprocess in the existing `sessions.test.ts` style.

## Steps

1. Run `bun run test` filtered to `sessions.test.ts` and `harnesses.test.ts`.
2. Expect: a session started with a mapped harness receives `session/set_config_option` with the
   level's `{configId, value}`; a supervision change mid-run pushes the option to every open
   session of that project and no other; the mapping round-trips through the harness record as
   data, prefilled from the shipped presets; a record with no mapping sends nothing.
