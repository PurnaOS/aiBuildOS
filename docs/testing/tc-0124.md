---
type: TestCase
id: TC-0124
title: "Invoking a command sends the harness's text and reads like a command"
state: draft
owner: srini
provenance: agent
created: 2026-08-21
generated: { by: "claude-code", at: 2026-08-21T00:00:00Z }
kind: automated
binding: "apps/desktop/e2e/playbooks.spec.ts"
tags: [composer, commands, transcript, e2e]
links:
  verifies: [RQ-0051#AC-2, ST-0069]
---

# TC-0124 — Invoking a command sends the harness's text and reads like a command

Verifies [RQ-0051](../requirements/rq-0051.md) AC-2: the wire carries exactly what the harness
expects, and the transcript renders a command card, not prose.

## Steps

1. Run `bun run test:e2e` filtered to the playbooks spec (build first).
2. Invoke an advertised command against the echo stub. Expect the echoed prompt to prove the
   exact `/name` text crossed the wire, and the transcript to render the invocation as
   RQ-0031's command card rather than an ordinary user message.
