---
type: TestCase
id: TC-0105
title: "Inbound links are buttons, and the dead props are gone"
state: active
owner: srini
provenance: agent
created: 2026-08-21
generated: { by: "claude-code", at: 2026-08-21T00:00:00Z }
kind: automated
binding: "apps/desktop/src/renderer/src/workspace/recordRail.test.tsx"
tags: [ui, chrome]
links:
  verifies: [RQ-0041#AC-1, RQ-0041#AC-3]
---

# TC-0105 — Inbound links are buttons, and the dead props are gone

Verifies [RQ-0041](../requirements/rq-0041.md) AC-1 and AC-3 at the component level: the rail's
inbound relationships render as pressable buttons wired to the `onOpen` already in Row's props,
and the two callbacks nothing ever passed no longer exist to advertise behaviour nobody wired.

## Steps

1. Render a rail row for an artifact with inbound edges. Expect each inbound ID to render as a
   button, and pressing one to call `onOpen` with that ID — not inert text.
2. Expect `NowTab` to declare no `onPrompt` prop and `ReviewTab` no `onOpen` prop — the types
   alone fail the build if a dead prop returns.
