---
type: TestCase
id: TC-0069
title: "Two builds at once; cancelling one leaves the other"
state: draft
owner: srini
provenance: agent
created: 2026-08-20
generated: { by: "claude-code", at: 2026-08-20T00:00:00Z }
kind: automated
binding: "apps/desktop/e2e/parallel.spec.ts"
tags: [parallel, worktrees, e2e]
links:
  verifies: [RQ-0021#AC-1, RQ-0021#AC-2, RQ-0021#AC-4]
---

# TC-0069 — Two builds at once; cancelling one leaves the other

Verifies [RQ-0021](../requirements/rq-0021.md) AC-1, AC-2 and AC-4 through the running
application, two stub agents in two worktrees (`slow` beside `file-writer`).

## Steps

1. Start two builds. Expect two distinct worktrees on disk at once, the slow one's story at
   `building`, and both builds on Now as live rows — the fast one's `building` is a blink the
   turn-end walk flips before anything can observe it, which is the walk doing its job.
2. Cancel the slow one. Expect its row saying so, the other build at `review` on disk and on the
   board, and nothing about the cancelled build touching its sibling.
