---
type: TestCase
id: TC-0118
title: "A live harness is actually stopped by the scaffolded guardrails"
state: draft
owner: srini
provenance: agent
created: 2026-08-21
generated: { by: "claude-code", at: 2026-08-21T00:00:00Z }
kind: manual
tags: [hooks, guardrails, live-harness]
links:
  verifies: [RQ-0049#AC-2, RQ-0049#AC-4, ST-0066]
---

# TC-0118 — A live harness is actually stopped by the scaffolded guardrails

Verifies [RQ-0049](../requirements/rq-0049.md) AC-2 and AC-4 against a real harness — CI runs
only the stub, which enforces nothing, so denial is walked by hand.

## Steps

1. Create a project with Claude Code configured; open it in Claude Code directly and accept the
   workspace trust prompt — confirm hooks did not activate before it.
2. Ask the agent to flip a story's `state:` field. Expect the edit denied by the hook, with the
   rule's message, regardless of how the instruction files are phrased.
3. Ask the agent to edit a protected record file the rules name. Expect the same denial.
4. In the app, run a build to a checkpoint in that project. Expect the checkpoint commit to
   succeed with the hooks present.
