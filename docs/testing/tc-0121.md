---
type: TestCase
id: TC-0121
title: "Capabilities are kept, extensions negotiated, payloads forwarded whole"
state: draft
owner: srini
provenance: agent
created: 2026-08-21
generated: { by: "claude-code", at: 2026-08-21T00:00:00Z }
kind: automated
binding: "packages/acp/src/session.test.ts"
tags: [acp, meta, unit]
links:
  verifies: [RQ-0052#AC-3, RQ-0052#AC-4, ST-0068]
---

# TC-0121 — Capabilities are kept, extensions negotiated, payloads forwarded whole

Verifies [RQ-0052](../requirements/rq-0052.md) AC-3 and AC-4 at the packages/acp seams, against
the stub subprocess.

## Steps

1. Run `bun run test` filtered to `packages/acp`.
2. Expect: `AgentSession` retains `agentCapabilities` from initialize (no longer discarded), and
   an extension advertised under `_meta` is queryable; the bridge forwards extension CUSTOM
   events with their payloads intact (no `_meta` stripping on the extension path); a stub in
   baseline mode advertises nothing and the session reports the extension absent; all traffic
   uses ACP methods and CUSTOM events — no new IPC channel appears in the contract.
