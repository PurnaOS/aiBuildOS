---
type: TestCase
id: TC-0084
title: "A command streams into the transcript and folds when long"
state: active
owner: srini
provenance: agent
created: 2026-08-20
generated: { by: "claude-code", at: 2026-08-20T00:00:00Z }
kind: automated
binding: "apps/desktop/e2e/terminal.spec.ts"
tags: [transcript, terminal, e2e]
links:
  verifies: [RQ-0031#AC-1, RQ-0031#AC-2, RQ-0031#AC-3, RQ-0031#AC-4]
---

# TC-0084 — A command streams into the transcript and folds when long

Verifies [RQ-0031](../requirements/rq-0031.md) AC-1 to AC-4 through the running application,
against a stub mode that streams multi-chunk execute output.

## Steps

1. Expect the command visible while output still streams in, chunk by chunk.
2. Expect the outcome in words with a duration when it ends.
3. The stub's own output never runs past a screenful, so the fold itself — the count, and "show
   all" unfolding it — is TC-0083's `visibleLines` coverage instead; what this file proves is the
   command and its live output actually reaching the running card.
