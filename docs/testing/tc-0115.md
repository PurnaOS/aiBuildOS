---
type: TestCase
id: TC-0115
title: "Three pinned surfaces, one Column"
state: draft
owner: srini
provenance: agent
created: 2026-08-21
generated: { by: "claude-code", at: 2026-08-21T00:00:00Z }
kind: automated
binding: "apps/desktop/src/renderer/src/workspace/tabStrip.test.tsx"
tags: [ui, surfaces, boards]
links:
  verifies: [RQ-0045#AC-1, RQ-0045#AC-3]
---

# TC-0115 — Three pinned surfaces, one Column

Verifies [RQ-0045](../requirements/rq-0045.md) AC-1 and AC-3 at the component level: the pinned
model is exactly the three surfaces, the tab vocabulary no longer admits what died, and both
boards compose from the one shared `boards/Column.tsx` rather than carrying forks of it.

## Steps

1. Build the tab strip's pinned model. Expect exactly Chat, Plan and Work, in that order, and
   expect `TabKind` to no longer include `now` — the types alone fail the build if it returns.
2. Render the Plan and Work boards. Expect both to compose their columns and cards from
   `boards/Column.tsx` — the same component identity in both, not two near-copies.
3. Render the Work surface. Expect the work board under the sprint header, with no tab strip of
   its own inside it.
