---
type: TestCase
id: TC-0073
title: "The flip lands in the build's own project, whatever is on screen"
state: draft
owner: srini
provenance: agent
created: 2026-08-20
generated: { by: "claude-code", at: 2026-08-20T00:00:00Z }
kind: automated
binding: "apps/desktop/src/main/builds.test.ts"
tags: [worktrees, record, main]
links:
  verifies: [BG-0007, RQ-0021]
---

# TC-0073 — The flip lands in the build's own project, whatever is on screen

Verifies [BG-0007](../bugs/bg-0007.md) against [RQ-0021](../requirements/rq-0021.md), in main,
where the fix lives — no window state anywhere in the test.

## Steps

1. Run a build's turn to its end with no renderer at all. Expect the story at `review` in the
   build's own project, through the guarded edit.
2. Two projects, same story ID, a build in the first: expect the first's story flipped and the
   second's untouched.
3. Expect the shared guarded-edit core to refuse an illegal flip exactly as the save handler does.
