---
type: Playbook
id: PB-0004
title: "Run the checks"
state: active
owner: {{OWNER}}
provenance: agent
created: 2026-08-20
generated: { by: "claude-code", at: 2026-08-20T00:00:00Z }
---

# PB-0004 — Run the checks

Run each of these and report what happened; the exit code is the whole verdict, not what got
printed.

No default check exists that every project can run — this template ships one placeholder fence
naming a command that does not exist, so running it honestly reports "could not run" instead of a
false pass. Replace it with your project's real gate: a test runner, a linter, whatever command's
exit code you trust — one fence per command.

```check
replace-this-with-your-projects-check-command
```

## Run

If this project serves something worth clicking through, declare how it runs (RQ-0025): a
` ```run ` fence, two lines — the command, then the `http(s)://` URL it serves once it's up. No
fence, no Preview offered on a story's review — that includes a malformed one, which is what this
placeholder deliberately is (its second line is not a URL, so it stays uncounted until you replace
both lines with your project's real command and address).

```run
replace-this-with-your-projects-run-command
replace-this-with-the-http-url-it-serves
```
